<Window x:Class="MJPEGStreamPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MJPEGStreamPlayer"
        xmlns:uc="clr-namespace:MJPEGStreamPlayer.View"
        xmlns:vm="clr-namespace:MJPEGStreamPlayer.ViewModel"
        mc:Ignorable="d"
        Title="MJPEGStreamPlayer" Height="540" Width="640">

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

   
    
    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <uc:SingleFrameView DataContext="{Binding Cell0}" Grid.Row="0" Grid.Column="0"/>
        
     


    </Grid>

</Window>